// Giữ tên/đường dẫn cũ để các nơi đang import không phải đổi; triển khai thực tế đã chuyển sang
// component dùng chung dựng trên Dialog (đóng bằng Esc + click nền). Xem [[ConfirmDialog]].
export { ConfirmDialog as ConfirmModal } from '@/components/shared/ConfirmDialog'
